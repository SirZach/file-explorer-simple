{{#if isPicture}}
    <img src="{{{fullPath}}}"></img>
{{/if}}

{{#if isMovie}}
    <video src="{{{fullPath}}}" type="video/mp4" controls>
        Your browser does not support the <code>video</code> element.
    </video>
{{/if}}